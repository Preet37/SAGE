# SAGE — environment template. Copy to backend/.env and fill in.
# Never commit real values; .gitignore protects .env files.

ANTHROPIC_API_KEY=
ASI1_API_KEY=
JWT_SECRET=replace-with-a-long-random-string
DATABASE_URL=sqlite:///./sage.db
AGENT_SEED=
AGENT_PORT=8001
