# ── Secrets & credentials ─────────────────────────────────────────────
# NEVER commit these. Loaded via os.getenv / process.env at runtime.
.env
.env.*
!.env.example
*.env
secrets/
secrets.*
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*_rsa
*_rsa.pub
id_*
.netrc
credentials.json
service-account*.json
*.keyfile

# Provider-specific key patterns we use
*ANTHROPIC*KEY*
*ASI1*KEY*
*FETCH*KEY*
*GROQ*KEY*
*OPENAI*KEY*
agent_seed.txt
agentverse-token*

# Claude Code local state (may contain captured keys / transcripts)
.claude/settings.local.json
.claude/transcripts/
.claude/cache/

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.eggs/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# SAGE local SQLite
sage.db
sage.db-journal
backend/sage.db*

# ── Node / Next.js ────────────────────────────────────────────────────
node_modules/
.next/
out/
.turbo/
.swc/
*.tsbuildinfo
next-env.d.ts

# ── Logs / OS / editor ────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# ── Playwright (existing) ─────────────────────────────────────────────
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# ── Build artifacts ───────────────────────────────────────────────────
dist/
build/
*.tar.gz

# ── Vendored / scratch dirs (out of SAGE submission scope) ────────────
hackathon_tools/
hack_build_v1/
