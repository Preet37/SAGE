# Source: https://arxiv.org/abs/2307.03172
# Author: Nelson F. Liu et al.
# Title: Lost in the Middle: How Language Models Use Long Contexts
# Fetched via: search
# Date: 2026-04-09

Abstract:While recent language models have the ability to take long contexts as input, relatively little is known about how well they use longer context.
We analyze the performance of language models on two tasks that require identifying relevant information in their input contexts: multi-document question answering and key-value retrieval.
We find that performance can degrade significantly when changing the position of relevant information, indicating that current language models do not robustly make use of information in long input contexts.
In particular, we observe that performance is often highest when relevant information occurs at the beginning or end of the input context, and significantly degrades when models must access relevant information in the middle of long contexts, even for explicitly long-context models.
Our analysis provides a better understanding of how language models use their input context and provides new evaluation protocols for future long-context language models.
|Comments:|18 pages, 16 figures.
Accepted for publication in Transactions of the Association for Computational Linguistics (TACL), 2023|
|--|--|
|Subjects:|Computation and Language (cs.CL)|
|Cite as:|arXiv:2307.03172 [cs.CL]|
| |(or arXiv:2307.03172v3 [cs.CL] for this version)|
| |https://doi.org/10.48550/arXiv.2307.03172 arXiv-issued DOI via DataCite|

Abstract
While recent language models have the abil-
ity to take long contexts as input, relatively
little is known about how well they use
longer context. We analyze the performance
of language models on two tasks that require
identifying relevant information in their in-
put contexts: multi-document question an-
swering and key-value retrieval. We find that
performance can degrade significantly when
changing the position of relevant informa-
tion, indicating that current language models
do not robustly make use of information in
long input contexts. In particular, we observe
that performance is often highest when rele-
vant information occurs at the beginning or
end of the input context, and significantly
degrades when models must access relevant
information in the middle of long contexts,
even for explicitly long-context models. Our
analysis provides a better understanding of
how language models use their input context
and provides new evaluation protocols for
future long-context language models.
1
Introduction
Language models have become an important and
flexible building block in a variety of user-facing
language technologies, including conversational
interfaces, search and summarization, and collabo-
rative writing (Shuster et al., 2022; Thoppilan et al.,
2022; Lee et al., 2022, inter alia). These models
perform downstream tasks primarily via prompting:
all relevant task specification and data to process is
formatted as a textual input context, and the model
returns a generated text completion. These input
contexts can contain thousands of tokens, espe-
cially when language models are used to process
long documents (e.g., legal or scientific documents,
conversation histories, etc.) or when language mod-

…

gpt-3.5-turbo-0613
gpt-3.5-turbo-0613 (closed-book)
Figure 1: Changing the location of relevant information
(in this case, the position of the passage that answers an
input question) within the language model’s input con-
text results in a U-shaped performance curve—models
are better at using relevant information that occurs at the
very beginning (primacy bias) or end of its input context
(recency bias), and performance degrades significantly
when models must access and use information located
in the middle of its input context.
relevant documents from a search engine, database
query results, etc; Petroni et al., 2020; Ram et al.,
2023; Shi et al., 2023; Mallen et al., 2023; Schick
et al., 2023, inter alia).
Handling these use-cases requires language mod-
els to successfully operate over long sequences. Ex-
isting language models are generally implemented
with Transformers (Vaswani et al., 2017), which re-

…

We empirically investigate this question via
controlled experiments with a variety of state-of-
the-art open (MPT-30B-Instruct, LongChat-13B
(16K)) and closed (OpenAI’s GPT-3.5-Turbo and
Anthropic’s Claude-1.3) language models in set-
tings that require accessing and using information
within an input context. In particular, our experi-
ments make controlled changes to the input context
size and the position of the relevant information
within the input context and study their effects on
language model performance. If language models
can robustly use information within long input con-
texts, then their performance should be minimally
affected by the position of the relevant information
in the input context.
We first experiment with multi-document ques-
tion answering, which requires models to reason
over provided documents to find relevant informa-
tion and use it to answer a given question; this task
mimics the retrieval-augmented generation setup

…

within the input context by changing the order of
the documents to place the relevant document at
the beginning, middle or end of the context.
We find that changing the position of relevant
information in the input context can substantially
affect model performance, indicating that current
language models do not robustly access and use
information in long input contexts. Furthermore,
we observe a distinctive U-shaped performance
curve (Figure 1); language model performance is
highest when relevant information occurs at the
very beginning (primacy bias) or end of its in-
put context (recency bias), and performance sig-
nificantly degrades when models must access and

…

the closed-book setting; 56.1%). Furthermore, we
find that models often have identical performance
to their extended-context counterparts, indicating
that extended-context models are not necessarily
better at using their input context (§2.3).
Given that language models struggle to retrieve
and use relevant information in the multi-document
question answering task, to what extent can lan-
guage models even retrieve from their input con-
texts? We study this question with a synthetic key-
value retrieval task, which is designed to be a mini-
mal testbed for the basic ability to retrieve matching
tokens from the input context. In this task, models

…

tecture (decoder-only vs. encoder-decoder), query-
aware contextualization, and instruction fine-tuning
(§4). We find that:
• Encoder-decoder models are relatively robust
to changes in the position of relevant informa-
tion within their input context, but only when
evaluated on sequences within its training-
time sequence length. When evaluated on
sequences longer than those seen during train-
ing, we observe a U-shaped performance
curve (§4.1).
• Query-aware contextualization (placing the
query before and after the documents or key-
value pairs) enables near-perfect performance
on the synthetic key-value task, but minimally
changes trends in multi-document QA (§4.2).
• Even base language models (i.e., without in-
struction fine-tuning) show a U-shaped per-
formance curve as we vary the position of
relevant information in the input context.
Our results indicate that prompting language
models with longer input contexts is a trade-off—
providing the language model with more informa-
tion may help it perform the downstream task, but
it also increases the amount of content that the
model must reason over, potentially decreasing ac-
curacy. To better understand this trade-off in prac-
tice, we perform a case study with retriever-reader

…

GPT-3.5-Turbo and ∼1% for claude-1.3).
Our analysis provides a better understanding of
how language models use their input context and
introduces new evaluation protocols for future long-
context models; to claim that a language model can
robustly use information within long input con-
texts, it is necessary to show that its performance
is minimally affected by the position of the rele-
vant information in the input context (e.g., minimal
difference in best- and worst-case performance).
To facilitate further work on understanding and
improving how language models use their input
context, we release our code and evaluation data.1
2
Multi-Document Question Answering
Our goal is to better understand how language mod-
els use their input context. To this end, we analyze
model performance on multi-document question
answering, which requires models to find relevant
information within an input context and use it to
answer the question. In particular, we make con-

…

Our experimental setup is similar to the needle-
in-a-haystack experiments of Ivgi et al. (2023), who
compare question answering performance when the
relevant paragraph is placed (i) at the beginning of
the input or (ii) a random position within the in-
put. They find that encoder-decoder models have
significantly higher performance when relevant in-
formation is placed at the start of the input context.
In contrast, we study finer-grained changes in the
position of relevant information.
2.2
Models
We analyze several state-of-the-art open and closed
language models. We use greedy decoding when
generating outputs and leave exploration of other

…

document question answering performance. Lower positions are closer to the start of the input context. Performance
is highest when relevant information occurs at the very start or end of the context, and rapidly degrades when models
must reason over information in the middle of their input context.
(16K).5 GPT-3.5-Turbo has a maximum context

…

document question answering performance when
varying the position of relevant information within
the input context. To contextualize model perfor-
mance, we also evaluate on the closed-book and
oracle settings (Table 1). In the closed-book setting,
models are not given any documents in their input
context, and must rely on their parametric memory
to generate the correct answer. On the other hand,
in the oracle setting, language models are given the
single document that contains the answer and must
use it to answer the question.
Model performance is highest when relevant in-
formation occurs at the beginning or end of its
input context.
As illustrated in Figure 5, chang-

…

in 20- and 30-document settings is lower than per-
formance without any input documents (i.e., closed-
book performance; 56.1%). These results indicate
that current models cannot effectively reason over
their entire context window when prompted for
downstream tasks.
Extended-context models are not necessarily
better at using input context.
When the input
context fits in the context window of both a model
and its extended-context counterpart, we see that
performance between them is nearly identical. For
example, the 10- and 20-document settings both
fit in the context window of GPT-3.5-Turbo and
GPT-3.5-Turbo (16K), and we observe that their
performance as a function of position of relative
information is nearly superimposed (solid purple
and dashed brown series in Figure 5). These results

…

3
How Well Can Language Models
Retrieve From Input Contexts?
Given that language models struggle to retrieve
and use information from the middle of their input
contexts in the multi-document question answering
task, to what extent can they simply retrieve from
input contexts? We study this question with a syn-
thetic key-value retrieval task, which is designed to

…

3.2
Results and Discussion
We experiment with input contexts containing
75, 140, and 300 key-value pairs (500 examples
each). We use the same set of models as the multi-
document question answering experiments, see
§2.2 for more details.
Figure 7 presents key-value retrieval perfor-

…

longchat-13b-16k
Figure 7: The effect of changing the input context length and the position of relevant information on key-value
retrieval performance. Lower positions are closer to the start of the input context. Although some models show
perfect accuracy on this synthetic task (e.g., Claude-1.3 and Claude-1.3 (100K)), we see again that performance is
often highest when relevant information is occurs at the very start or end of the context, and rapidly degrades when
models must retrieve from the middle of the input context.
placed at the start of the input context, LongChat-
13B (16K) tends to generate code to retrieve the
key, rather than outputting the value directly.
4
Why Are Language Models Not Robust
to Changes in the Position of Relevant
Information?
Our multi-document question answering and key-
value retrieval results show that language models
struggle to robustly access and use information in
long input contexts, since performance degrades
significantly when changing the position of rele-
vant information. To better understand why, we per-
form some preliminary investigations into the role
of model architecture (decoder-only vs. encoder-
decoder), query-aware contextualization, and in-
struction fine-tuning.
4.1
Effect of Model Architecture
The open models we evaluated are all decoder-only
models—at each timestep, they may only attend
to prior tokens. To better understand the poten-
tial effects of model architecture on how language
model use context, we compare decoder-only and
encoder-decoder language models.
We experiment with Flan-T5-XXL (Raffel et al.,
2020; Chung et al., 2022) and Flan-UL2 (Tay et al.,

…

coder) before instruction fine-tuning on sequences
with 2048 tokens in the encoder and 512 tokens
in the decoder. However, since these models use
relative positional embeddings, they can (in prin-
ciple) extrapolate beyond these maximum context
lengths; Shaham et al. (2023) find that both mod-

…

in the position of relevant information within the
input context (1.9% absolute difference between
best- and worst-case performance). When evalu-
ated on settings with sequences longer than 2048
tokens (Figure 8; center and right), Flan-UL2 per-
formance begins to degrade when relevant informa-

…

robust to changes in the position of relevant information within their input context (left subplot). In contrast, when
these models are evaluated on sequences longer than those seen during training (center and right subplots), we
observe a U-shaped performance curve—performance is higher when relevant information occurs at the beginning
or end of the input context, as opposed to the middle of the input context.

…

and responses. The task specification and/or in-
struction is commonly placed at the beginning of
the input context in supervised instruction fine-
tuning data, which might lead instruction fine-
tuned language models to place more weight on
the start of the input context. To better understand
the potential effects of instruction fine-tuning on
how language models use long input contexts, we
compare the multi-document question answering
performance of MPT-30B-Instruct against its base
model (i.e., before instruction fine-tuning) MPT-
30B. We use the same experimental setup as §2.
Figure 10 compares the multi-document QA
performance of MPT-30B and MPT-30B-Instruct
as a function of the position of the relevant in-

…

Although the absolute performance of MPT-30B-
Instruct is uniformly higher than that of MPT-30B,
their overall performance trends are similar. We
also observe that instruction fine-tuning slightly re-
duces the worst-case performance disparity from
nearly 10% between the base model best- and
worst-case performance to around 4%.
These observations complement prior work,
which found that non-instruction fine-tuned lan-
guage models are biased towards recent tokens (i.e.,
the end of the input context; Khandelwal et al.,
2018; Press et al., 2021). This recency bias has
been observed in past work when evaluating mod-
els on next-word prediction of contiguous text, a
setting where language models minimally benefit
from long-range information (Sun et al., 2021). In
contrast, our results show that language models
are capable of using longer-range information (i.e.,
the beginning of the input context) when prompted
with instruction-formatted data. We hypothesize
that non-instruction fine-tuned language models
learn to use these long contexts from similarly-
formatted data that may occur in Internet text seen
during pre-training, e.g., StackOverflow questions
and answers.
To better understand the effect of additional fine-
tuning and model scale, we also experimented
with Llama-2 models of varying sizes (7B, 13B,
and 70B) with and without additional supervised
fine-tuning and reinforcement learning from hu-
man feedback (Appendix E). We find that the U-
shaped performance curve only appears in suffi-
ciently large language models (with or without ad-

…

Our results indicate that prompting language mod-
els with longer input contexts is a trade-off—
providing the language model with more informa-
tion may help it perform the downstream task, but
it also increases the amount of content that the
model must reason over, potentially decreasing
accuracy. Even if a language model can take in

…

ments from Wikipedia with the highest relevance
score. To condition language models on these re-
trieved documents, we simply include them in the
prompt. We evaluate retriever recall and reader
accuracy (whether any of the annotated answers
appear in the predicted output) as a function of the
number of retrieved documents k. We use a subset
of NaturalQuestions-Open where the long answer
is a paragraph (as opposed to a table or a list).
Figure 11 presents retriever recall and open-

…

Turbo and ∼1% for Claude-1.3), while significantly
increasing the input context length (and thus la-
tency and cost). These results, coupled with the
observation that models are often better at retriev-
ing and using information at the start or end of
the input contexts, suggest that effective rerank-

##### Abstract
While recent language models have the ability to take long contexts as input, relatively little is known about how well they use longer context.
We analyze the performance of language models on two tasks that require identifying relevant information in their input contexts: multi-document question answering and key-value retrieval.
We find that performance can degrade significantly when changing the position of relevant information, indicating that current language models do not robustly make use of information in long input contexts.
In particular, we observe that performance is often highest when relevant information occurs at the beginning or end of the input context, and significantly degrades when models must access relevant information in the middle of long contexts, even for explicitly long-context models.
Our analysis provides a better understanding of how language models use their input context and provides new evaluation protocols for future long-context language models.
…
- **Bibkey:**
- **Cite (ACL):**
Nelson F. Liu, Kevin Lin, John Hewitt, Ashwin Paranjape, Michele Bevilacqua, Fabio Petroni, and Percy Liang.
2024. Lost in the Middle: How Language Models Use Long Contexts.
*Transactions of the Association for Computational Linguistics*, 12:157–173.

Abstract
While recent language models have the abil-
ity to take long contexts as input, relatively
little is known about how well they use
longer context. We analyze the performance
of language models on two tasks that require
identifying relevant information in their in-
put contexts: multi-document question an-
swering and key-value retrieval. We find that
performance can degrade significantly when
changing the position of relevant informa-
tion, indicating that current language models
do not robustly make use of information in
long input contexts. In particular, we observe
that performance is often highest when rele-
vant information occurs at the beginning or
end of the input context, and significantly
degrades when models must access relevant
information in the middle of long contexts,
even for explicitly long-context models. Our
analysis provides a better understanding of
how language models use their input context
and provides new evaluation protocols for
future long-context language models.
1
Introduction
Language models have become an important and
flexible building block in a variety of user-facing
language technologies, including conversational
interfaces, search and summarization, and collabo-
rative writing (Shuster et al., 2022; Thoppilan et al.,
2022; Lee et al., 2022, inter alia). These models
perform downstream tasks primarily via prompting:
all relevant task specification and data to process is
formatted as a textual input context, and the model
returns a generated text completion. These input
contexts can contain thousands of tokens, espe-
cially when language models are used to process
long documents (e.g., legal or scientific documents,
conversation histories, etc.) or when language mod-

…

gpt-3.5-turbo-0613
gpt-3.5-turbo-0613 (closed-book)
Figure 1: Changing the location of relevant information
(in this case, the position of the passage that answers an
input question) within the language model’s input con-
text results in a U-shaped performance curve—models
are better at using relevant information that occurs at the
very beginning (primacy bias) or end of its input context
(recency bias), and performance degrades significantly
when models must access and use information located
in the middle of its input context.
relevant documents from a search engine, database
query results, etc; Petroni et al., 2020; Ram et al.,
2023; Shi et al., 2023; Mallen et al., 2023; Schick
et al., 2023, inter alia).
Handling these use-cases requires language mod-
els to successfully operate over long sequences. Ex-
isting language models are generally implemented
with Transformers (Vaswani et al., 2017), which re-

…

We empirically investigate this question via
controlled experiments with a variety of state-of-
the-art open (MPT-30B-Instruct, LongChat-13B
(16K)) and closed (OpenAI’s GPT-3.5-Turbo and
Anthropic’s Claude-1.3) language models in set-
tings that require accessing and using information
within an input context. In particular, our experi-
ments make controlled changes to the input context
size and the position of the relevant information
within the input context and study their effects on
language model performance. If language models
can robustly use information within long input con-
texts, then their performance should be minimally
affected by the position of the relevant information
in the input context.
We first experiment with multi-document ques-
tion answering, which requires models to reason
over provided documents to find relevant informa-
tion and use it to answer a given question; this task
mimics the retrieval-augmented generation setup

…

within the input context by changing the order of
the documents to place the relevant document at
the beginning, middle or end of the context.
We find that changing the position of relevant
information in the input context can substantially
affect model performance, indicating that current
language models do not robustly access and use
information in long input contexts. Furthermore,
we observe a distinctive U-shaped performance
curve (Figure 1); language model performance is
highest when relevant information occurs at the
very beginning (primacy bias) or end of its in-
put context (recency bias), and performance sig-
nificantly degrades when models must access and

…

the closed-book setting; 56.1%). Furthermore, we
find that models often have identical performance
to their extended-context counterparts, indicating
that extended-context models are not necessarily
better at using their input context (§2.3).
Given that language models struggle to retrieve
and use relevant information in the multi-document
question answering task, to what extent can lan-
guage models even retrieve from their input con-
texts? We study this question with a synthetic key-
value retrieval task, which is designed to be a mini-
mal testbed for the basic ability to retrieve matching
tokens from the input context. In this task, models

…

tecture (decoder-only vs. encoder-decoder), query-
aware contextualization, and instruction fine-tuning
(§4). We find that:
• Encoder-decoder models are relatively robust
to changes in the position of relevant informa-
tion within their input context, but only when
evaluated on sequences within its training-
time sequence length. When evaluated on
sequences longer than those seen during train-
ing, we observe a U-shaped performance
curve (§4.1).
• Query-aware contextualization (placing the
query before and after the documents or key-
value pairs) enables near-perfect performance
on the synthetic key-value task, but minimally
changes trends in multi-document QA (§4.2).
• Even base language models (i.e., without in-
struction fine-tuning) show a U-shaped per-
formance curve as we vary the position of
relevant information in the input context.
Our results indicate that prompting language
models with longer input contexts is a trade-off—
providing the language model with more informa-
tion may help it perform the downstream task, but
it also increases the amount of content that the
model must reason over, potentially decreasing ac-
curacy. To better understand this trade-off in prac-
tice, we perform a case study with retriever-reader

…

texts, it is necessary to show that its performance
is minimally affected by the position of the rele-
vant information in the input context (e.g., minimal
difference in best- and worst-case performance).
To facilitate further work on understanding and
improving how language models use their input
context, we release our code and evaluation data.1
2
Multi-Document Question Answering
Our goal is to better understand how language mod-
els use their input context. To this end, we analyze
model performance on multi-document question
answering, which requires models to find relevant
information within an input context and use it to
answer the question. In particular, we make con-

…

Our experimental setup is similar to the needle-
in-a-haystack experiments of Ivgi et al. (2023), who
compare question answering performance when the
relevant paragraph is placed (i) at the beginning of
the input or (ii) a random position within the in-
put. They find that encoder-decoder models have
significantly higher performance when relevant in-
formation is placed at the start of the input context.
In contrast, we study finer-grained changes in the
position of relevant information.
2.2
Models
We analyze several state-of-the-art open and closed
language models. We use greedy decoding when
generating outputs and leave exploration of other

…

document question answering performance when
varying the position of relevant information within
the input context. To contextualize model perfor-
mance, we also evaluate on the closed-book and
oracle settings (Table 1). In the closed-book setting,
models are not given any documents in their input
context, and must rely on their parametric memory
to generate the correct answer. On the other hand,
in the oracle setting, language models are given the
single document that contains the answer and must
use it to answer the question.
Model performance is highest when relevant in-
formation occurs at the beginning or end of its
input context.
As illustrated in Figure 5, chang-

…

in 20- and 30-document settings is lower than per-
formance without any input documents (i.e., closed-
book performance; 56.1%). These results indicate
that current models cannot effectively reason over
their entire context window when prompted for
downstream tasks.
Extended-context models are not necessarily
better at using input context.
When the input
context fits in the context window of both a model
and its extended-context counterpart, we see that
performance between them is nearly identical. For
example, the 10- and 20-document settings both
fit in the context window of GPT-3.5-Turbo and
GPT-3.5-Turbo (16K), and we observe that their
performance as a function of position of relative
information is nearly superimposed (solid purple
and dashed brown series in Figure 5). These results

…

3
How Well Can Language Models
Retrieve From Input Contexts?
Given that language models struggle to retrieve
and use information from the middle of their input
contexts in the multi-document question answering
task, to what extent can they simply retrieve from
input contexts? We study this question with a syn-
thetic key-value retrieval task, which is designed to

…

3.2
Results and Discussion
We experiment with input contexts containing
75, 140, and 300 key-value pairs (500 examples
each). We use the same set of models as the multi-
document question answering experiments, see
§2.2 for more details.
Figure 7 presents key-value retrieval perfor-

…

longchat-13b-16k
Figure 7: The effect of changing the input context length and the position of relevant information on key-value
retrieval performance. Lower positions are closer to the start of the input context. Although some models show
perfect accuracy on this synthetic task (e.g., Claude-1.3 and Claude-1.3 (100K)), we see again that performance is

…

4
Why Are Language Models Not Robust
to Changes in the Position of Relevant
Information?
Our multi-document question answering and key-
value retrieval results show that language models
struggle to robustly access and use information in
long input contexts, since performance degrades
significantly when changing the position of rele-
vant information. To better understand why, we per-
form some preliminary investigations into the role
of model architecture (decoder-only vs. encoder-
decoder), query-aware contextualization, and in-
struction fine-tuning.
4.1
Effect of Model Architecture
The open models we evaluated are all decoder-only
models—at each timestep, they may only attend
to prior tokens. To better understand the poten-
tial effects of model architecture on how language
model use context, we compare decoder-only and
encoder-decoder language models.
We experiment with Flan-T5-XXL (Raffel et al.,
2020; Chung et al., 2022) and Flan-UL2 (Tay et al.,

…

in the position of relevant information within the
input context (1.9% absolute difference between
best- and worst-case performance). When evalu-
ated on settings with sequences longer than 2048
tokens (Figure 8; center and right), Flan-UL2 per-
formance begins to degrade when relevant informa-

…

robust to changes in the position of relevant information within their input context (left subplot). In contrast, when
these models are evaluated on sequences longer than those seen during training (center and right subplots), we
observe a U-shaped performance curve—performance is higher when relevant information occurs at the beginning
or end of the input context, as opposed to the middle of the input context.

…

how language models use long input contexts, we
compare the multi-document question answering
performance of MPT-30B-Instruct against its base
model (i.e., before instruction fine-tuning) MPT-
30B. We use the same experimental setup as §2.
Figure 10 compares the multi-document QA
performance of MPT-30B and MPT-30B-Instruct
as a function of the position of the relevant in-

…

Although the absolute performance of MPT-30B-
Instruct is uniformly higher than that of MPT-30B,
their overall performance trends are similar. We
also observe that instruction fine-tuning slightly re-
duces the worst-case performance disparity from
nearly 10% between the base model best- and

…

been observed in past work when evaluating mod-
els on next-word prediction of contiguous text, a
setting where language models minimally benefit
from long-range information (Sun et al., 2021). In
contrast, our results show that language models
are capable of using longer-range information (i.e.,
the beginning of the input context) when prompted
with instruction-formatted data. We hypothesize
that non-instruction fine-tuned language models
learn to use these long contexts from similarly-
formatted data that may occur in Internet text seen
during pre-training, e.g., StackOverflow questions
and answers.
To better understand the effect of additional fine-
tuning and model scale, we also experimented
with Llama-2 models of varying sizes (7B, 13B,
and 70B) with and without additional supervised
fine-tuning and reinforcement learning from hu-
man feedback (Appendix E). We find that the U-
shaped performance curve only appears in suffi-
ciently large language models (with or without ad-

…

Our results indicate that prompting language mod-
els with longer input contexts is a trade-off—
providing the language model with more informa-
tion may help it perform the downstream task, but
it also increases the amount of content that the
model must reason over, potentially decreasing
accuracy. Even if a language model can take in

…

ments from Wikipedia with the highest relevance
score. To condition language models on these re-
trieved documents, we simply include them in the
prompt. We evaluate retriever recall and reader
accuracy (whether any of the annotated answers
appear in the predicted output) as a function of the
number of retrieved documents k. We use a subset
of NaturalQuestions-Open where the long answer
is a paragraph (as opposed to a table or a list).
Figure 11 presents retriever recall and open-

## use longer context. We analyze language model

performance on two tasks that require identify- 65 ing relevant information within their input con- texts: multi-document question answering and key-value retrieval. We find that performance is 60 often highest when relevant information occurs at the beginning or end of the input context, 55 and significantly degrades when models must access relevant information in the middle of 1st 5th 10th 15th 20th long contexts.
Furthermore, performance sub- Position of Document with the Answer stantially decreases as the input context grows gpt-3.5-turbo-0613 (open-book) longer, even for explicitly long-context models. gpt-3.5-turbo-0613 (closed-book) Our analysis provides a better
understanding of how language models use their input con- Figure 1: Changing the location of relevant information text and provides new evaluation protocols for (in this case, the position of the passage that answers an future long-context models. input question) within the language model’s input con- text results in a U-shaped performance
curve—models 1 Introduction are better at using relevant information that occurs at the Language models have become an important and very beginning or end of its input context, and perfor- mance degrades significantly when models must access flexible building block in a variety of user-facing and use information located in the middle of its input language technologies, including conversational context.
For example, GPT-3.5-Turbo’s open-book per- interfaces, search and summarization, and collabo- formance on the multi-document question task when rative writing. These models perform downstream relevant information is placed in the middle of its input tasks primarily via prompting: all relevant task context is lower than its performance when predicting specification and data to process is formatted as without any documents (i.e., the closed-book setting; a textual context, and the model returns a gener- 56.1%).
See Figure 5 for full results. ated text completion. These input contexts can contain thousands of tokens, especially when using Language models are generally implemented with language models on lengthy inputs (e.g., legal or Transformers, which scale poorly to long sequences scientific documents, conversation histories, etc.) (e.g., since self-attention complexity is quadratic or augmenting them with external information (e.g., with the input sequence length).

…

; Handling these use-cases requires language mod- Poli et al., 2023; Rubin and Berant, 2023, inter els to successfully operate over long sequences. alia) have resulted in language models with larger * Work partially completed as an intern at Samaya AI. context windows, but it remains unclear how theseextended-context language models make use their mal testbed for the basic ability to retrieve matchinginput contexts when performing downstream tasks. tokens from the input context.

…

We first experiment with multi- information. We observe a similar U-shaped perfor-document question answering, which requires mod- mance curve in this setting; many models struggleels to reason over provided documents to find rele- to simply retrieve matching tokens that occur in thevant information and use it to answer a given ques- middle of their input context.tion; this task mimics the retrieval-augmented gen- To better understand why language models strug-eration setup underlying many commercial gener- gle to access and use information in the middleative search and question answering applications of their input contexts, we conduct preliminary(e.g., Bing Chat).
We make controlled changes to investigations into the role of model architecturethe input context size and the position of the rele- (decoder-only vs. encoder-decoder), query-awarevant information within the input context and study contextualization, and instruction fine-tuning (§5).their effects on model performance.
In particular, We find that encoder-decoder models are relativelywe can increase the input context length by adding robust to changes in the position of relevant in-more documents to the input context (akin to re- formation within their input context when evalu-trieving more documents in retrieval-augmented ated on sequences within its training-time sequencegeneration), and modify the position of the relevant length, but they show a U-shaped curve when eval-information within the context by changing the or- uated on sequences longer than those seen duringder of the documents in the input context to place training (§5.1).
In addition, query-aware contex-the relevant document at the beginning, middle or tualization (placing the query before and after theend of the context. documents or key-value pairs) enables models to We observe a distinctive U-shaped performance, perform the synthetic key-value task perfectly, butwhich can be clearly visualized in Figure 1, as minimally changes trends in multi-document QAwe vary the position of the relevant information (§5.2).
Finally, even base language models (i.e.,—language model performance is highest when rel- without instruction fine-tuning) show a U-shapedevant information occurs at the very beginning or performance curve as we vary the position of rele-end of its input context, and performance signifi- vant information in the input context.cantly degrades when models must access and use Lastly, we perform a case study with retriever-information in the middle of their input context reader models on open-domain question answering(§3.3).
For example, when relevant information to better understand the trade-off between addingis placed in the middle of its input context, GPT- more information to an input context and increas-3.5-Turbo’s performance on the multi-document ing the amount of content that the model must rea-question task is lower than its performance when son over (§6)—in contrast to our controlled multi-predicting without any documents (i.e., the closed- document QA task, where the context always con-book setting; 56.1%).

…

We study this question with a synthetic key- improves performance (∼1.5% for GPT-3.5-Turbovalue retrieval task, which is designed to be a mini- and ∼1% for claude-1.3). Our analysis provides a better understanding of answering, which requires models to find relevanthow language models use their input context and information within an input context and using it tointroduces new evaluation protocols for future long- answer the question.
In particular, we make con-context models. To facilitate further work on un- trolled changes to the length of the input contextderstanding and improving how language models and the position of the relevant information anduse their input context, we release our code and measure changes in task performance.evaluation data at nelsonliu.me/papers/lost-in-the-middle.
3.1 Experimental Setup Our multi-document question answering task2 Language Models closely parallels the retrieval-augmented genera-We study language models as functions that take tion setup underlying commercial search and ques-a textual input context and return a textual out- tion answering applications (e.g., Bing Chat). Input. Modern language models are most commonly these experiments, the model inputs are (i) a ques-implemented with Transformers (Vaswani et al., tion to answer and (ii) k documents (e.g., passages2017).
Transformer language models encode in- from Wikipedia), where exactly one the documentsput contexts with self-attention, whose time and contains the answer to the question and k − 1 “dis-memory complexity is quadratic in the length of tractor” documents do not. Performing this taskthe input, limiting their application to very long requires the model to access the document that con-sequences.
As a result, language models are gen- tains the answer within its input context and useerally pre-trained with relatively small amount of it to answer the question. Figure 2 presents anprior context (its context window), which accord- example.ingly also limits the maximum length of their input We instantiate this task with data from thecontexts.

…

Following Kandpal et al. (2022) and Mallen et al. We analyze several state-of-the-art open and closed(2023), we use accuracy as our primary evaluation models. We use greedy decoding when generatingmetric, judging whether any of the correct answers outputs and leave exploration of other decoding(as taken from the NaturalQuestions annotations) methods to future work.
We use a standard set ofappear in the predicted output. prompts for each model (depicted in Figure 2). To modulate the input context length in this task, Open models. We experiment with MPT-30B-we increase or decrease the number of retrieved Instruct, which has a maximum context length ofdocuments that do not contain the answer (Fig- 8192 tokens.

…

Figure 5: The effect of changing the position of relevant information (document containing the answer) on multi- document question answering performance. Lower positions are closer to the start of the input context. Performance is generally highest when relevant information is positioned at the very start or very end of the context, and rapidly degrades when models must reason over information in the middle of their input context.

…

We evaluate claude-1.3 and claude- curve in model performance—models are much1.3-100k with the Anthropic API; claude-1.3 has a better at identifying and using relevant informa-maximum context length of 8K tokens, and claude- tion that occurs at the very beginning and very1.3-100k has an extended context length of 100K end of contexts, and suffer degraded performancetokens.4 when forced to use information within the middle of its input context.

…

We claude-1.3 longchat-13b-16k use accuracy as our evaluation metric, assessing whether the correct value appears in the predictedFigure 6: Language model performance (averaged output.across position of relevant information) on the multi- Our synthetic key-value retrieval task is designeddocument question answering task decreases as the input to provide a minimal testbed for the basic abil-context grows longer. ity to retrieve matching tokens from an input con- text.

…

instead), since language features maycess longer input contexts, they may not be better present potential confounders (e.g., because Trans-at reasoning over the information within its context former language models may have varying sensi-window. tivity to different linguistic features in their inputExtended-context models are not necessarily bet- context; O’Connor and Andreas, 2021).ter at using input context.

…

75, 140, and 300 key-value pairs (500 examples each). We use the same set of models as the multi-Given that language models struggle to retrieve document question answering experiments, seeand use information from the middle of their input §3.2 for more details.contexts in the multi-document question answer- Although the synthetic key-value retrieval tasking task, to what extent can they simply retrieve only requires identifying exact match withinfrom input contexts?

…

The relevant key-value pair for correctlyanswering the request is bolded within the input context. 5 Why Do Language Models Struggle To Use Their Entire Input Context? Our multi-document question answering and key-retrieving keys from 140 or more key-value pairs. value retrieval results show that language model The results on the key-value retrieval task have performance degrades significantly when they mustlargely similar trends to the results on the multi- access relevant information in the middle of long in-document question-answering task (excepting mod- put contexts.
To better understand why, we performels with perfect performance on the key-value re- some preliminary investigations into the role oftrieval task). In particular, we see the U-shaped model architecture (e.g., decoder-only vs. encoder-performance curve again; model performance is decoder), query-aware contextualization, and thelowest when they must access key-value pairs in effects of instruction fine-tuning.the middle of their input context.

…

Figure 10: The effect of changing the input context length and the position of relevant information on key-value retrieval performance. Lower positions are closer to the start of the input context. Although some models are largely perfect on this synthetic task (e.g., claude-1.3 and claude-1.3), we see again that performance is often highest when relevant information is occurs at the very start or very end of the context, and rapidly degrades when models must retrieve from the middle of the input context.

…

Figure 11: Encoder-decoder models (Flan-UL2 and Flan-T5-XXL) are relatively robust to changes in the position of relevant information within their input context when evaluated on sequences that are shorter than their encoder’s training-time maximum sequence length (2048 and 512 tokens, respectively). However, when these models are evaluated on sequences longer than those seen during training (20- and 30-document settings), they also exhibit a U-shaped performance curve, where performance is much higher when the relevant information occurs at the beginning or end of the input context as opposed to the middle.

…

However, since these models use mance degradation when placing relevant infor- relative positional embeddings, they can (in prin- mation in the middle of the input context. ciple) extrapolate beyond these maximum context lengths; Shaham et al. (2023) find that both models We speculate that encoder-decoder models may can perform well with sequences of 8K tokens. make better use of their context windows because their bidirectional encoder allows processing each Figure 11 juxtaposes the performance of document in the context of future documents, po- decoder-only and encoder-decoder models.

…

Both modelswhen relevant information occurs at the very beginning, have a U-shaped performance curve, where performancebut slightly decreases performance otherwise. is much higher when relevant information occurs at the start or end of the input context, indicating that the instruction tuning process itself is not necessarily re-between documents. sponsible for these performance trends.

…

In thisin the position of relevant information in their in- supervised instruction-tuning data, the task specifi-put context—can use this intuition to also improve cation and/or instruction is commonly placed at thethe performance of decoder-only models by plac- beginning of the input context, which might leading the query before and after the data, enabling instruction-tuned language models to place morequery-aware contextualization of documents (or weight on the start of the input context.key-value pairs)?